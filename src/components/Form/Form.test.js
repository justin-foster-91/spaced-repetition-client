import React from 'react'
import ReactDOM from 'react-dom'
import { BrowserRouter } from 'react-router-dom';
import Form from './Form'

it('renders without crashing', () => {
  // Odd error, revisit later
})
